# Minnal Murali Help Portal — Final

Clean final build matching the finalized Gamma structure and hero artwork.

## Before upload

Open `config.js` and paste your working Formspree endpoint. Keep the same endpoint that already works in your current project.

## Upload

Replace the contents of the GitHub repository with every file in this folder. Do not keep old image files or old HTML/CSS files in the repository.

Required root files:
- index.html
- styles.css
- app.js
- config.js
- assets/gamma-hero.jpeg
- assets/gamma-hero-overlay.png
- assets/gamma-origin.png
- assets/gamma-closing.png
